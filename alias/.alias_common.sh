alias chmodx="chmod +x"
alias ls="ls --color"
alias lsg="ls | grep"
alias home="cd ~"

# Make process table searchable, pass in argument to search for
alias psg="ps aux | grep -v grep | grep -i -e VSZ -e"
