set nocompatible " Enable modern Vim features not compatible with Vi spec.
set undofile " Saves undo history across sessions.
set showmatch " When a bracket is typed show its match
set cursorline " Highlight the current line.
set shell=sh " I use ZSH, vim shoulnd't

set number

set splitbelow
set splitright
